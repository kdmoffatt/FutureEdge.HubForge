{
  "name": "futureedge-hubforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "hubforge": "pnpm --filter @hubforge/cli run cli --",
    "hubforge:build": "pnpm --filter @hubforge/cli run build",
    "hubforge:regen": "pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/project-regenerate.ps1",
    "hubforge:regen:sh": "bash ./scripts/project-regenerate.sh"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}