{
  "name": "futureedge-hubforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "hubforge": "pnpm --filter @hubforge/cli run cli --",
    "hubforge:build": "pnpm --filter @hubforge/cli run build",
    "hubforge:regen": "pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/fieldops-regenerate.ps1",
    "hubforge:regen:sh": "bash ./scripts/fieldops-regenerate.sh",
    "fieldops:regen": "pnpm hubforge:regen",
    "fieldops:regen:sh": "pnpm hubforge:regen:sh"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}